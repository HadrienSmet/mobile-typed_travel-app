import { StyleSheet } from "react-native";

import { SHADES, SIZES, WEIGHTS } from "../../../constants";

const styles = StyleSheet.create({
    label: {
        color: SHADES.black07,
        fontSize: SIZES.small,
        fontWeight: WEIGHTS.bold,
        letterSpacing: 0.5,
    }
});

export default styles;
